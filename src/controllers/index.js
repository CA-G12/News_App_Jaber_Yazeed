const { addFavs, deleteFav, getFavs } = require("./favs");
const { getSources, getNews } = require("./news");

module.exports = {
  addFavs,
  deleteFav,
  getFavs,
  getSources,
  getSources,
  getNews,
};
